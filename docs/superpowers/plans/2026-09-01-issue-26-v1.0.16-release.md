# Issue #26 v1.0.16 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish SkillDock v1.0.16 with the Windows GitHub backup subprocess-window fix and notify the Issue #26 reporter after release assets are complete.

**Architecture:** Keep the product change limited to applying the existing hidden-subprocess configuration to backup snapshot Git metadata commands. Release from the merged `main` commit using the repository's signed local publish script, then let the release workflow add Windows assets.

**Tech Stack:** Rust, Tauri 2, npm, Git, GitHub CLI, GitHub Actions

**Spec:** https://github.com/wanghuan9/skilldock/issues/26

## Global Constraints

- Release version is `1.0.16` and tag is `v1.0.16`.
- The feature branch must first track the same-named remote branch.
- The release commit must be merged and pushed to `main` before publishing.
- macOS artifacts must be signed and notarized; Windows x64 artifacts must be produced by `.github/workflows/release.yml`.
- Do not close Issue #26 until the reporter confirms the Windows result.

---

### Task 1: Finalize the fix and release metadata

**Files:**
- Modify: `src-tauri/src/backup_snapshot.rs`
- Modify: `package.json`
- Modify: `package-lock.json`
- Modify: `src-tauri/Cargo.toml`
- Modify: `src-tauri/Cargo.lock`
- Modify: `src-tauri/tauri.conf.json`
- Create: `docs/release/notes/v1.0.16.md`

**Interfaces:**
- Consumes: `crate::library::configure_hidden_subprocess(&mut Command)`
- Produces: version-aligned v1.0.16 source and curated release notes

- [ ] **Step 1: Set all application version fields to `1.0.16`**

  Update the five version-bearing files required by `docs/release/updater.md` without changing dependency versions.

- [ ] **Step 2: Add the curated release note**

  ```markdown
  ## 修复

- 修复 Windows 上 GitHub 云备份整理本地 Skill 目录时反复弹出 Git 控制台窗口的问题。
  ```

- [ ] **Step 3: Run local verification**

  Run: `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`

  Run: `cargo test --manifest-path src-tauri/Cargo.toml --lib`

  Run: `npm test`

  Run: `npm run build`

  Expected: all commands exit successfully.

### Task 2: Publish the source branches

**Files:**
- Modify: Git history only

**Interfaces:**
- Consumes: verified v1.0.16 working tree
- Produces: pushed feature branch and merged/pushed `main` release commit

- [ ] **Step 1: Commit and push the feature branch**

  Run: `git push -u origin feature/issue-26-windows-backup-git-console`

- [ ] **Step 2: Update and merge main**

  Run `git switch main`, `git pull --ff-only`, merge the feature branch, and push `main`.

- [ ] **Step 3: Verify remote identity**

  Confirm local `HEAD`, `origin/main`, and the feature merge result identify the same release source commit.

### Task 3: Publish and verify v1.0.16

**Files:**
- Create: signed GitHub Release assets and tag through existing release automation

**Interfaces:**
- Consumes: clean pushed `main` at version `1.0.16`
- Produces: GitHub Release `v1.0.16`, updater metadata, macOS and Windows artifacts

- [ ] **Step 1: Run the signed local publisher**

  Run: `npm run release:publish`

  Review the generated curated release notes and continue only when they match `docs/release/notes/v1.0.16.md`.

- [ ] **Step 2: Wait for the release workflow**

  Wait for the `Release` workflow triggered by `v1.0.16` to complete successfully.

- [ ] **Step 3: Verify published artifacts**

  Confirm the tag points to the merged release commit and the Release contains `latest.json`, the macOS Apple Silicon artifacts, and the Windows x64 installer plus signature. Confirm `latest.json` version is `1.0.16` and contains both supported platforms.

### Task 4: Notify the reporter

**Files:**
- Modify: GitHub Issue #26 discussion only

**Interfaces:**
- Consumes: verified v1.0.16 release URL and assets
- Produces: user-facing reply with upgrade and validation instructions

- [ ] **Step 1: Comment on Issue #26**

  Explain that v1.0.16 applies Windows hidden-process creation to the previously missed backup snapshot Git metadata path, link the release, and ask the reporter to verify backup completion and absence of `git.exe` windows.

- [ ] **Step 2: Keep the issue open**

  Do not close the issue until the reporter confirms the result.
