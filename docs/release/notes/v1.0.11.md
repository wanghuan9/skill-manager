## 新增

- Skills 工作区支持添加、编辑和持久化标签，并可按标签筛选；列表、分组和卡片视图会保留各自的展示偏好。

## 优化

- 优化 Skill 标签界面在列表、卡片和详情中的布局与配色，未设置标签时使用一致的透明虚线按钮样式。
- 统一 Skill 来源、卡片摘要和“启用到工具”标签的软件顺序，常用工具优先展示 Claude Code、Codex、Cursor 和 OpenCode。

## 修复

- 修复标签按钮移动后测试仍依赖旧标题元素顺序的问题。
