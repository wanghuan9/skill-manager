## 新增

- 云端备份节点支持添加、修改和清空备注，方便标记发布前等关键工作区快照。
- 支持将任意可用备份节点单独下载为 ZIP 镜像，无需先恢复到本地。

## 优化

- 备份历史默认显示最近 5 个节点，可通过弹窗底部固定的“加载更多”按钮继续查看历史，并显示已加载数量。
- 优化备份备注编辑交互，使用独立橘色区分备注，鼠标悬停时显示“添加备注”；节点按钮统一排列为恢复、下载、删除。
- Skill 列表日常刷新和状态变更时保持顺序稳定，仅在新增条目时重新应用排序。
